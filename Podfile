# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftUI Companion' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUI Companion
  pod 'R.swift'

  target 'SwiftUI CompanionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUI CompanionUITests' do
    # Pods for testing
  end

end
